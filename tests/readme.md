Tests using the `pytest` system utility.

Requirements:
- unix system
- python 3 installed
- pytest installed

To run the tests:

```bash
    cd /you/warlock/directory/tests
    pytest
```

or run within the Warlock docker container:

```bash
    make up
    make pytests
```